version: '3.8'

services:
  gateway-api-spring:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      SPRING_CLOUD_COMPATIBILITY_VERIFIER_ENABLED: "false"