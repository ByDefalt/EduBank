spring:
  datasource:
    url: jdbc:mariadb://localhost:3383/operationapi
    driver-class-name: org.mariadb.jdbc.Driver
    username: operationuser
    password: operationpassword

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
